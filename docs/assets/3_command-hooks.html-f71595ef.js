import{_ as e}from"./plugin-vue_export-helper-c27b6911.js";import{o as t,c as a,a as r}from"./app-ccf1f840.js";const o={},n=r('<p>PURISTA provides transformers and hooks for commands.</p><h2 id="transformer" tabindex="-1"><a class="header-anchor" href="#transformer" aria-hidden="true">#</a> Transformer</h2><p>The idea of having transformers, follows the PURISTA principal to separate things and decouple business logic from technical needs.<br> Transformers are responsible to convert the raw message payload in/to the data shape &amp; type, the command function is expecting.</p><p>Use transformers for:</p><ul><li>converting data format (example xml to js object to xml)</li><li>decrypt and encrypt the payload (end-to-end encryption)</li></ul><h3 id="input-transformer" tabindex="-1"><a class="header-anchor" href="#input-transformer" aria-hidden="true">#</a> Input transformer</h3><p>If a input transform is set, it will be executed as very first step in the message handling.<br> The input transformer has it&#39;s own schemas for input payload and parameters.<br> If the schema validation fails, an error response will automatically created and sent back to the caller. The command guards and the command function are not executed, if this validations fails. The error will be a <code>HandledError</code> with status of <code>Bad Request</code>, as the input is not as expected.</p><p>If the transform function itself is throwing an error, other than <code>HandledError</code>, a <code>UnhandledError</code> with status <code>Internal Server Error</code> is sent back to the caller.</p><p>If the transformer is used for decryption, and the decryption fails, the transform function should throw a <code>HandledError</code> with a status of <code>Unauthorized</code> or <code>Not permitted</code>.</p><p>The transform function must return a object with <code>payload</code> and <code>parameter</code> property. The type of these two properties is generated and set, based on the command function input schema and command function parameter schema. Because of this, the input transformer must be defined after the command function schemas.</p><h3 id="output-transformer" tabindex="-1"><a class="header-anchor" href="#output-transformer" aria-hidden="true">#</a> Output transformer</h3><p>If a output transform is set, it will be executed as very last step in the message handling.<br> The output transformer has it&#39;s own schema, which will validate the returned value of the transform function.<br> If the schema validation fails, an error response will automatically created and sent back to the caller. The command guards and the command function are not executed, if this validations fails.</p><p>If the transform function itself is throwing an error, other than <code>HandledError</code>, a <code>UnhandledError</code> with status <code>Internal Server Error</code> is sent back to the caller.</p><p>The transform function must return the final raw payload. Because of this, the output transformer must be defined after the command function schemas.</p><h2 id="guards" tabindex="-1"><a class="header-anchor" href="#guards" aria-hidden="true">#</a> Guards</h2><p>The intention of guards is, to have the opportunity, to move authentication and authorization logic out of the main business logic.<br> A common example is, to add role and permission checks by using guards.</p><p>You can add multiple values to command function. Guards are executed in parallel after transformers and before the command function is executed.<br> Guards should throw a <code>HandledError</code> with a proper status code, if the execution should be aborted.<br> If a guard is throwing an error, other than a <code>HandledError</code>, a <code>UnhandledError</code> with status <code>Internal Server Error</code> is sent back to the caller.</p><p>A guard should not change values and does not return a value.</p>',18),s=[n];function d(i,c){return t(),a("div",null,s)}const u=e(o,[["render",d],["__file","3_command-hooks.html.vue"]]);export{u as default};
