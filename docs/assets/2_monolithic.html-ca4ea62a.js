import{_ as a}from"./plugin-vue_export-helper-c27b6911.js";import{r as s,o as l,c as r,b as e,d as t,e as o,w as c,a as d}from"./app-cbf1ff6b.js";const h="/graphic/monolithic.svg",u={},p=e("p",null,"It is the most mature and straightforward way, to simply deploy multiple instances to have horizontal scaling and to provide some kind of failover solution.",-1),m=e("p",null,[e("img",{src:h,alt:"example"})],-1),f=e("p",null,"This solution works for a single developer team or local, development and test environments. It perfectly fits for early stages if the final decision about infrastructure/cloud provider is not made. If not enough DevOps resources are available, this might be the preferred way. Also, costs for running the application are predictable - price per instance. The costs do not directly correlate to the usage amount of executing single functions. This approach does not need a real cloud provider and you can choose cheap (dedicated) servers.",-1),g=e("p",null,"To have a scalable solution, there is a need to ensure that the message broker and the HTTP proxy/load balancer do not become a single point of failure. They need to be fault tolerant and should be able to scale or at least handle the number of requests.",-1),v=e("p",null,"The overall workload is distributed across all running instances. In the picture above you can see, that every user request is sent to the HTTP server service of one of the instances. The HTTP server itself invokes the correlating command of the matching service. The invoked service might be executed on the same instance or a different instance. This depends on the message broker and the timing. The command execution result will be sent to the instance, which was receiving the user's request and will respond to the user's request.",-1),b=e("p",null,'Subscriptions are triggered by messages from the message broker. This means they are executed at "any of" the instances.',-1),_=d('<p>If you need some kind of <strong>state or session handling</strong>: Because the execution of commands and subscription is generally done on &quot;any of the instances&quot; (random!), state handling will need some shared state store, even if a single user session is pinned to a specific instance!</p><p>The simple and common solution, to advise the load balancer/proxy, to pin the user&#39;s session to always the same instance, and to store the session data simply in the memory of that instance, will <strong>not work</strong> properly! Only the same HTTP server service will be invoked, but the command, corresponding to called HTTP endpoint, might be executed on any of the available instances.</p><p>You will need some additional external, shared-state storage!</p><div class="hint-container tip"><p class="hint-container-title">Pros</p><ul><li>simple in the basic setup</li><li>can run on bare metal with no additional overhead (IoT/edge devices)</li><li>low configuration</li><li>scales workload</li><li>provides low-level backup</li><li>different geo locations possible</li><li>ensures all services (schemas, db migrations...) are in sync</li><li>can be done at nearly any provider</li></ul></div><div class="hint-container danger"><p class="hint-container-title">Cons</p><ul><li>the need to deploy always all services</li><li>different access levels, restrictions, and policies for services &amp; functions is not possible</li><li>instances (incl. all services &amp; functions) are running 24/7</li><li>most likely a lot of manual steps</li><li>the (manual) effort for managing instances grows, with the number of instances in most cases</li><li>every addition, like state handling, secret, and config stores, must also be provided &amp; managed</li><li>code must be in mono repo or additional effort to merge all repos to a single deployment bundle</li></ul></div>',5);function y(w,T){const n=s("RouterLink"),i=s("Badge");return l(),r("div",null,[p,e("p",null,[t("In the picture below, a simplified setup with two instances is shown. You can scale it up by adding more instances or scale down, to one instance, if you think of "),o(n,{to:"/handbook/6._scale/2_edge_device.html"},{default:c(()=>[t("IoT and small edge devices")]),_:1}),t(".")]),m,f,g,v,b,o(i,{text:"Be aware",type:"warning"}),_])}const I=a(u,[["render",y],["__file","2_monolithic.html.vue"]]);export{I as default};
