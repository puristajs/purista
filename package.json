{
  "version": "1.7.4",
  "private": true,
  "scripts": {
    "start": "npm run dev -w examples/fullexample",
    "build": "turbo run build",
    "build:dev": "turbo run build:dev",
    "lint": "turbo run lint --parallel",
    "test": "turbo run test",
    "dev:doc": "npm run dev -w website",
    "bump:major": "npm version major --no-git-tag-version",
    "bump:minor": "npm version minor --no-git-tag-version",
    "bump:patch": "npm version patch --no-git-tag-version",
    "update:doc": "npm run gitclean && typedoc && git add --all && git commit -S -am \"doc: update api documentation\" && npm run document -w website && git-cliff > CHANGELOG.md && git commit -am \"doc: update CHANGELOG\"",
    "release:major": "turbo run gitclean && npm run release:prepare && npm run update:doc && turbo run gitclean && turbo run bump:major && npm run commit:major && npm publish --workspaces",
    "release:minor": "turbo run gitclean && npm run release:prepare && npm run update:doc && turbo run gitclean && turbo run bump:minor && npm run commit:minor && npm publish --workspaces",
    "release:patch": "turbo run gitclean && npm run release:prepare && npm run update:doc && turbo run gitclean && turbo run bump:patch && npm run commit:patch && npm publish --workspaces",
    "gitclean": "./scripts/isGitClean.sh",
    "release:prepare": "turbo run lint build test",
    "tag": "git push && git push --tags && rm -rf build/temp",
    "commit:major": "npm version major --no-git-tag-version && scripts/commitVersion.sh && npm run build",
    "commit:minor": "npm version minor --no-git-tag-version && scripts/commitVersion.sh && npm run build",
    "commit:patch": "npm version patch --no-git-tag-version && scripts/commitVersion.sh && npm run build",
    "vuepress": "npm run vuepress -w website",
    "check:cycling": "npx madge --circular --exclude '\\.d\\.ts$' --extensions ts ./packages",
    "sign": "git rebase --exec \"git commit --amend --no-edit -n -S\" -i"
  },
  "workspaces": [
    "./packages/*",
    "./examples/*",
    "./website"
  ],
  "devDependencies": {
    "@swc/jest": "^0.2.26",
    "@types/jest": "^29.5.1",
    "@types/node": "^18.16.19",
    "@types/sinon": "^10.0.15",
    "@typescript-eslint/eslint-plugin": "^5.61.0",
    "@typescript-eslint/parser": "^5.61.0",
    "esbuild": "^0.18.11",
    "eslint": "^8.44.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-config-standard": "^17.1.0",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-json": "^3.1.0",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "5.0.0-alpha.2",
    "eslint-plugin-simple-import-sort": "^10.0.0",
    "git-cliff": "^1.2.0",
    "jest": "^29.6.0",
    "prettier": "^3.0.0",
    "rimraf": "^5.0.1",
    "sinon": "^15.2",
    "testcontainers": "^9.9.1",
    "turbo": "1.10.7",
    "typedoc": "^0.24.8",
    "typedoc-plugin-markdown": "^3.15.3",
    "typescript": "^5.1.6"
  },
  "packageManager": "npm@9.6.2"
}
