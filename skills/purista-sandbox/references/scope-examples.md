Use sandbox scope to control reuse:

- omitted scope: shared per tenant + project + user
- `agent-run`: isolate parallel runs
- `conversation`: isolate one chat thread
- `custom`: application-defined branch or review workspace
