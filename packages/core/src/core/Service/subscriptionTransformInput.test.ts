describe('subscriptionTransformInput', () => {
	it.todo('throws HandledError if parameter schema validation fails')
	it.todo('throws HandledError if payload schema validation fails')
	it.todo('throws HandledError if output schema validation fails')
})
