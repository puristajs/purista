import { AmqpBridgeConfig } from '@purista/amqpbridge'
import { getNewInstanceId } from '@purista/core'

const amqpBridgeConfig: AmqpBridgeConfig = {
  defaultCommandTimeout: 30000,
  exchangeName: 'purista',
  namePrefix: 'purista',
  url: 'amqp://localhost',
  instanceId: getNewInstanceId(),
  encoder: {},
  encrypter: {},
}

export default amqpBridgeConfig
