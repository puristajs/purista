import { ServiceBuilder, ServiceInfoType } from '@purista/core'

import { general{{properCase serviceName}}ServiceInfo } from '../general{{properCase serviceName}}ServiceInfo'

export const {{camelCase serviceName}}ServiceInfo: ServiceInfoType = {
  serviceVersion: '1',
  ...general{{properCase serviceName}}ServiceInfo,
}

// create a service builder instance only.

export const {{camelCase serviceName}}ServiceBuilder = new ServiceBuilder({{camelCase serviceName}}ServiceInfo)
