import { {{camelCase serviceName}}ServiceBuilder } from './{{camelCase serviceName}}ServiceBuilder'
import { {{camelCase serviceName}}ServiceV{{version}}ConfigSchema, {{camelCase serviceName}}ServiceV{{version}}DefaultConfig } from './{{camelCase serviceName}}ServiceConfig'

// bring service config definition, command definitions and subscription definitions together in the service

export const {{camelCase serviceName}}V{{version}}Service = {{camelCase serviceName}}ServiceBuilder.setConfigSchema({{camelCase serviceName}}ServiceV{{version}}ConfigSchema)
  .setDefaultConfig({{camelCase serviceName}}ServiceV{{version}}DefaultConfig)
