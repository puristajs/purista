import { extendApi } from '@anatine/zod-openapi'
import { z } from 'zod'

// define the input parameters
export const {{camelCase service.name}}V{{service.version}}{{properCase name}}InputParameterSchema = extendApi(z.object({}), { title: '{{name}} input parameter schema' })

// define the input payload
export const {{camelCase service.name}}V{{service.version}}{{properCase name}}InputPayloadSchema = extendApi(z.any(), { title: '{{name}} input payload schema' })

// define the output payload
export const {{camelCase service.name}}V{{service.version}}{{properCase name}}OutputPayloadSchema = extendApi(z.any(), { title: '{{name}} output payload schema' })
