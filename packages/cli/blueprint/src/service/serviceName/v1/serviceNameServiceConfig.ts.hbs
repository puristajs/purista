import { z } from 'zod'

// define the service config schema and the default service configuration

export const {{camelCase name}}ServiceV{{version}}ConfigSchema = z.object({})

export type {{properCase name}}ServiceV{{version}}Config = z.input<typeof {{camelCase name}}ServiceV{{version}}ConfigSchema>
