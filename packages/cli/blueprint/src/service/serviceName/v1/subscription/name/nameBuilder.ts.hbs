import { {{camelCase service.name}}ServiceBuilder } from '../../{{camelCase service.name}}ServiceBuilder'
import { {{camelCase service.name}}V{{service.version}}{{properCase name}}InputPayloadSchema } from './schema'

export const {{camelCase name}}SubscriptionBuilder = {{camelCase service.name}}ServiceBuilder.getSubscriptionBuilder(
  '{{camelCase name}}',
  '{{description}}',
){{#if (isSet subscriptionEventName)}}.subscribeToEvent('{{trim subscriptionEventName}}')
{{/if}}{{#if (isSet subscriptionEventList)}}.subscribeToEvent('{{trim subscriptionEventList}}')
{{/if}}
  .addPayloadSchema({{camelCase service.name}}V{{service.version}}{{properCase name}}InputPayloadSchema)
  .setSubscriptionFunction(async function (_context, _payload, _parameter) {
    // add your business logic here
  })
