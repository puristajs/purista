import { ServiceBuilder, ServiceInfoType } from '@purista/core'

import { general{{properCase name}}ServiceInfo } from '../general{{properCase name}}ServiceInfo'

export const {{camelCase name}}ServiceInfo: ServiceInfoType = {
  serviceVersion: '1',
  ...general{{properCase name}}ServiceInfo,
}

// create a service builder instance only.

export const {{camelCase name}}ServiceBuilder = new ServiceBuilder({{camelCase name}}ServiceInfo)
