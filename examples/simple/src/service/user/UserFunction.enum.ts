/**
 * Enum for all functions of user service.
 * Function names must be unique within one service.
 * It is also recommended that functions names are globally unique.
 **/
export enum UserFunction {
  SignUpNewUser = 'signUpNewUser',
}
