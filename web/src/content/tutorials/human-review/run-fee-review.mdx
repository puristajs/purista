---
title: Run the review service
description: Exercise the main-app session path and generated Hono routes through the banking integration test.
order: 161
status: published
---

Run each generated command test as you replace its starter schemas, then
exercise the main application's session-backed path with the focused test:

```bash title="Run the fee-change review integration test"
npm test -- --run src/service/bankingFeeChangeReview/v1/command
npx vitest run examples/banking/src/review/service.integration.test.ts
npm run test -w @purista/banking-tutorials
```

The banking integration test signs in Dana and Erin through the local opaque
session flow, then uses the generated endpoints under `/api/v1/`.

The first proposal is pending. No fee changes when a proposal is created.
Next, define the information that a reviewer needs to decide.
