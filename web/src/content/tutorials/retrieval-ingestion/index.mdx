---
title: Build a complete RAG pipeline
description: Ingest, embed, retrieve, and stream grounded answers with PURISTA and the native Harness.
order: 200
kind: chapter
group: ai
status: draft
---

Retrieval-augmented generation, usually shortened to RAG, gives a model
relevant application data before it writes an answer. A complete RAG feature
has two connected paths: an indexing path that prepares source documents and
an answer path that retrieves evidence for each question.

This chapter builds both paths. PURISTA owns the service boundaries,
authorization, HTTP endpoints, database resource, and address-first calls.
The native Harness owns the embedding and chat model contracts, the retrieval
agent, its tool contract, and the answer workflow.

```text title="The complete RAG path"
protected ingestion command
  -> collection edit guard
  -> chunk source text
  -> Harness embedding model
  -> PostgreSQL and pgvector resource

protected chat stream
  -> mounted Harness workflow
  -> retrieval agent chooses search_knowledge
  -> authorized PURISTA search command
  -> same Harness embedding model
  -> pgvector similarity search
  -> grounded answer with citations
  -> AI SDK UI Message Stream v1
```

The small Example Bank source document makes the result easy to try. The
lesson is about building RAG with PURISTA; it does not introduce a banking
domain model.

## What you will build

Follow these pages in order. Each build page starts from the previous page's
working checkpoint and ends with a command you can run.

### Build the pipeline

1. [Create the RAG project and model contracts](./build/configure-harness/).
2. [Build the protected ingestion and embedding command](./build/ingest/).
3. [Store documents and vectors in PostgreSQL with pgvector](./build/store/).
4. [Build authorized semantic retrieval with the same embedding model](./build/retrieve/).
5. [Let the native agent choose the command-backed search tool](./build/agent/).
6. [Expose portable agent events as an AI SDK UI Message Stream](./build/stream-ui/).
7. [Authenticate the generated HTTP endpoints](./build/authenticate-http/).
8. [Create the React, Tailwind, shadcn, and test shell](./build/ui-shell/).
9. [Build the ingestion and AI Elements chat interface](./build/ui/).
10. [Compose and run the complete application](./build/run/).

### Test the boundaries

11. [Test ingestion, embeddings, authorization, and search](./testing/test-rag/).
12. [Test the native Harness tool loop](./testing/test-harness/).
13. [Test streaming and HTTP authentication](./testing/test-stream-http/).
14. [Test pgvector storage and browser behavior](./testing/test-storage-ui/).

The complete source is in `examples/banking/chapters/retrieval-ingestion`.
This chapter stays a draft until PURISTA v4, Harness v3, and the matching CLI
and project creator are available from npm and a clean consumer replay
succeeds.

Start with [Create the RAG project and contracts](./build/configure-harness/).
