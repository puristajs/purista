---
title: Retrieve through the same model
description: Authorize search, create a query embedding through Harness, and run the tenant-scoped repository query.
order: 204
kind: lesson
status: draft
---

Stored and query vectors must use the same embedding contract. Replace the
generated search command with the complete implementation below. It declares
the same `embedding` alias as ingestion.

```ts title="src/service/knowledge/v1/command/searchKnowledge/searchKnowledgeCommandBuilder.ts" write
import { knowledgeHarness } from '../../harness/knowledgeHarnessMount.js'
import { knowledgeV1ServiceBuilder } from '../../knowledgeV1ServiceBuilder.js'
import { requireKnowledgeCollectionAccess } from '../../requireKnowledgeCollectionAccess.js'
import { searchKnowledgeInputSchema, searchKnowledgeOutputSchema } from '../../schema.js'

export const searchKnowledgeCommandBuilder = knowledgeV1ServiceBuilder
	.getCommandBuilder('searchKnowledge', 'Search authorized knowledge chunks for a grounded answer')
	.addPayloadSchema(searchKnowledgeInputSchema)
	.addOutputSchema(searchKnowledgeOutputSchema)
	.canUseHarnessModel(knowledgeHarness, 'embedding')
	.setBeforeGuardHooks({
		collectionAccess: async function (context, payload) {
			await requireKnowledgeCollectionAccess(context.resources.knowledgeCollectionPolicy, {
				tenantId: context.message.tenantId,
				principalId: context.message.principalId,
				collectionId: payload.collectionId,
				action: 'search',
			})
		},
	})
	.setCommandFunction(async function (context, payload) {
		const signal = new AbortController().signal
		const response = await context.model.embedding.embed(
			{ input: payload.query, dimensions: this.config.embeddingDimensions },
			signal,
		)
		const queryEmbedding = response.embeddings[0]?.vector
		if (!queryEmbedding) throw new Error('The embedding model returned no query vector')
		const matches = await context.resources.knowledgeRepository.search({
			tenantId: context.message.tenantId ?? '',
			collectionId: payload.collectionId,
			embeddingModel: this.config.embeddingModel,
			queryEmbedding: [...queryEmbedding],
			limit: payload.limit,
			signal,
		})
		return { matches }
	})
```
The `search` action is distinct from `edit`. A valid authenticated user may be
allowed to ask questions without being allowed to replace source material.
The business guard runs before the embedding request and database call.

The command is intentionally not exposed as a browser endpoint. It is the
service-owned retrieval capability used by the model-selected host tool on the
next page. Other services can also declare and invoke its versioned address.

```bash title="Check both indexing commands" replay="project"
npm run build:server
```

Continue with [Let the agent choose the search tool](./agent/).
