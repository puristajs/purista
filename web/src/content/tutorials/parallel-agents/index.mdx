---
title: Run specialist agents in parallel
description: Define focused agents, bound workflow fan-out, and merge typed evidence through a protected PURISTA command.
order: 260
kind: chapter
group: ai
status: draft
---

This chapter teaches bounded parallel agent work. One workflow asks a risk
specialist and a response-planning specialist to inspect the same support case.
Their independent, schema-validated results are merged in ordinary TypeScript.

The Example Bank case stays small so the framework behavior remains visible.
You will learn how to:

- give each specialist one responsibility and one output schema;
- allowlist child agents on the workflow;
- cap total and simultaneous child calls;
- use `context.fanOut` instead of unmanaged concurrency;
- merge evidence without asking another model to reinterpret it;
- apply business authorization at both the command and published workflow
  addresses.

1. [Create the project and generate every boundary](./setup/scaffold/)
2. [Define the specialist agents](./build/define-specialists/)
3. [Fan out with an explicit concurrency budget](./build/fan-out/)
4. [Protect and publish only the workflow](./build/merge-evidence/)
5. [Invoke the workflow from a PURISTA command](./build/invoke-workflow/)
6. [Compose the two model bindings](./build/compose-runtime/)
7. [Run the complete parallel workflow](./build/run-application/)
8. [Test both specialists and their workflow](./testing/test-parallel/)
9. [Test PURISTA command and EventBridge routing](./testing/test-routing/)

The retained project is in `examples/banking/chapters/parallel-agents`. Its
tests and default demo use two strict fake providers and a real EventBridge, so
they need no API key.

Start with [Create the parallel workflow project](./setup/scaffold/).
