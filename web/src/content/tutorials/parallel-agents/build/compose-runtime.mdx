---
title: Bind the named models
description: Create the Support service with separate riskModel and responseModel bindings.
order: 266
kind: lesson
status: published
---

Definitions name the models they need. Runtime composition supplies each provider and model. This keeps the agents portable and lets production use different models for different jobs.

```ts title="src/createSupportService.ts" write
import type { EventBridge, Logger } from '@purista/core'
import type { ModelProvider } from '@purista/harness'
import type { SupportCasePolicy } from './service/support/v1/SupportResources.js'
import { supportV1Service } from './service/support/v1/supportV1Service.js'

export function createSupportService(
	eventBridge: EventBridge,
	logger: Logger,
	options: Readonly<{
		supportCasePolicy: SupportCasePolicy
		riskModel: { provider: ModelProvider; model: string }
		responseModel: { provider: ModelProvider; model: string }
	}>,
) {
	return supportV1Service.getInstance(eventBridge, {
		logger,
		resources: { supportCasePolicy: options.supportCasePolicy },
		ai: {
			models: {
				riskAssessment: options.riskModel,
				responsePlanning: options.responseModel,
			},
		},
	})
}
```
The `ai.models` map binds both explicit purpose aliases: `riskAssessment` and `responsePlanning`.

The earlier `fanOut(..., { concurrency: 2 })` limit applies only to callbacks
inside that workflow step. Deployment admission is separate. A larger service
can set `ai.concurrency: { runs, modelCalls }` to limit complete root runs and
active provider calls. This small deterministic service does not need an
additional runtime limit.

```bash title="Check runtime composition" replay="project"
npm run build
```

Continue with [Run the demo and live service](../run-application/).
