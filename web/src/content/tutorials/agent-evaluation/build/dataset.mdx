---
title: Create the project and evaluation dataset
description: Scaffold the PURISTA boundary and store stable inputs, expected behavior, and semantic segments.
order: 291
kind: lesson
status: draft
---

Create a clean project and install its complete published manifest.

```bash title="Create and enter the evaluation project" replay="parent"
npm create purista@latest agent-evaluation -- --runtime node --event-bridge default --non-interactive --defaults
cd agent-evaluation
```

```json title="package.json" write
{
	"name": "@purista/tutorial-agent-evaluation",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"engines": {
		"node": ">=24.15.0"
	},
	"scripts": {
		"add:service": "purista add service",
		"add:command": "purista add command",
		"add:agent": "purista add agent",
		"build": "tsc --noEmit",
		"demo": "node --import tsx src/demo.ts",
		"evaluate": "node --import tsx src/runEvaluation.ts",
		"evaluate:live": "node --env-file-if-exists=.env --import tsx src/runLiveEvaluation.ts",
		"lint": "biome check .",
		"start": "node --env-file-if-exists=.env --import tsx src/index.ts",
		"test": "vitest run"
	},
	"dependencies": {
		"@purista/core": "4.0.0",
		"@purista/harness": "3.0.0",
		"@purista/harness-openai": "3.0.0",
		"zod": "4.4.3"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.8",
		"@purista/cli": "4.0.0",
		"@types/node": "26.2.0",
		"@types/sinon": "22.0.0",
		"sinon": "22.1.0",
		"tsx": "4.23.12",
		"typescript": "7.0.2",
		"vitest": "4.1.10"
	}
}
```
```bash title="Install the evaluation dependencies" replay="project"
npm install
```

Generate the Support owner and its normal application command. The portable
Harness definition is created on the next page and can also run standalone in
the evaluator.

```bash title="Generate the evaluated PURISTA boundary" replay="project"
npm run add:service -- support --description "Own support classification" --non-interactive
npm run add:command -- classify-support-message --service support --service-version 1 --description "Classify one support message" --non-interactive
```

Create a small versioned dataset. Each case has stable input, expected fields,
and a segment used for aggregate reporting. Expectations deliberately cover
behavior instead of provider-specific prose.

```ts title="src/dataset.ts" write
import type { EvaluationDataset } from '@purista/harness'
import type { ClassificationInput, ClassificationOutput } from './harness/support/supportHarness.js'

export type ClassificationAssessment = Pick<ClassificationOutput, 'category' | 'urgency'>

export const supportClassificationDataset = {
	id: 'support-classification-cases',
	version: '1.0.0',
	cases: [
		{
			id: 'missing-card',
			input: { messageId: 'message-1', text: 'My card was stolen and someone is using it now.' },
			assessment: { category: 'card', urgency: 'urgent' },
			segments: { category: 'card', urgency: 'urgent' },
		},
		{
			id: 'transfer-date',
			input: { messageId: 'message-2', text: 'When will my scheduled transfer arrive?' },
			assessment: { category: 'transfer', urgency: 'normal' },
			segments: { category: 'transfer', urgency: 'normal' },
		},
		{
			id: 'locked-out',
			input: { messageId: 'message-3', text: 'I am locked out and must pay rent today.' },
			assessment: { category: 'account_access', urgency: 'urgent' },
			segments: { category: 'account_access', urgency: 'urgent' },
		},
	],
} as const satisfies EvaluationDataset<ClassificationInput, ClassificationAssessment>
```
Continue with [Mount the evaluated agent](../mount-agent/).
